# 更新日志

只记录**用户可感知的变化**，不罗列内部重构。版本号规则：0.x 为预发布，待全流程实机验收通过后再发 1.0.0；每次发版 `+N`（versionCode）无条件 +1。

## v0.1.0 — 2026-09-24

首个可安装版本。目标机型：作业帮喵喵机 P1（203 dpi，名义有效打印宽度 384 点）。

### 新增

- **粘贴即用**：直接把 AI 生成的错题解答整段粘进来就能出图。裸 LaTeX（没有 `$` 包裹的 `\frac`、`\log` 之类）会自动识别成公式，不需要手动补定界符；页面上会实时显示「识别到 N 处公式」。
- **两种输出模式**：「只打印题干」与「题干 + 解答」，默认后者；选择会被记住。
- **打印校准向导**：打一张校准条即可回答点阵宽度、字号、细线保真等必答项，结果写回机型档案。所有参数在设置页也能手动改，可一键重置为默认值。
- **预览与分享**：出图前可预览排版结果，再通过系统分享面板把图片交给喵喵机官方 App 打印。

### 变更

- 输出图片宽度严格等于机型档案的点阵宽度（默认 384 点，1:1 不做缩放）。
- 二值化默认开启长游程结构线保护，避免分式分数线被打断。
- 可切换是否写入打印机分辨率元数据（打出来偏大偏小时改这一项）。

### 本版未验证

- **尚未在真机上打印验证**：分享链路、384 点宽度、细线保真、校准向导参数均按保守默认值实现，等实机打印后回填。首次安装后建议先跑一遍校准向导。
- 公式内的中文（`\text{中文}`）尚未拆分到正文通道，会以公式字体渲染。
- 教材符号（`▱ ≌ ∽ ⌢ ①②③` 等）未做完整覆盖断言，可能出现豆腐块。
- 极端超宽公式的处理策略未定稿，长公式的换行表现未实测。
- 化学式（`\ce{}`）暂不支持。
- 回归语料仅 1 条，**本次未跑语料回归**。
